@page
@model PriorityLifeWebInterface.Pages.Salesperson.Salesperson_AddModel
@section AdditionalJavaScript {
    @await Html.PartialAsync("_ValidationScriptsPartial")

    <script type="text/javascript">
        $(function () {
            $('.datetextbox').datepicker({dateFormat: 'm/d/yy'});
        });
    </script>
}
@{ 
    ViewData["Title"] = "Add Salesperson";
}
<h2>@ViewData["Title"]</h2>
@Html.ValidationSummary(true)
<div>
    @Html.Partial("./Shared/_AddEditSalespersonPartial", Model.PartialModel)
</div>
